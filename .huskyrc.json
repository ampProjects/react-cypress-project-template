{
  "hooks": {
    "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true",
    "pre-commit": "yarn format && yarn lint && git add -A .",
    "pre-push": "CI=true react-scripts test && yarn cypress:run"
  }
}
